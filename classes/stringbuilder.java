import java.util.*;
public class stringbuilder {
    public static void main(String[] args) {
        Scanner sc= new Scanner(System.in);
        //converting a string buidler to string
        //string buffer and string builder both have same functionality...
        //methods are same like append , replace 

        //the only difference between string builder and string buffer is 
        /*
         both string buffer and string builder are mutable
         not thread sofe 

         string buffer is relatively fast -- 
         string buuilder is thread safe

         string buffer is thread safe
         string buffer allow synchronization 
         

         they both are mutable

         */
        long a = sc.nextLong();
    }
}
